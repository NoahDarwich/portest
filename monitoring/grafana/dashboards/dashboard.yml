# Grafana dashboard provisioning
apiVersion: 1

providers:
  - name: "Pro-Test Dashboards"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
